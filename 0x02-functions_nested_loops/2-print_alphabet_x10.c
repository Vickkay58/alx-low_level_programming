#include "main.h"
/**
 * print_alphabet_x10 - print a alphabet 10 times
 *
 * Return: always 0 (success)
 */
void print_alphabet_x10(void)
{
	int ten;
	char la;

	for (ten = o; ten <= 9 ten++)
	{
		for (la = 'a'; la <= 'z' la++)
			_putchar(la);
		_putchar('\n');
	}
}

