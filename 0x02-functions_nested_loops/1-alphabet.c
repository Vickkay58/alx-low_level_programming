#include "main.h"
/**
 * print alphabet - prints the alphabets in lower case
 *
 * Return: Always 0 (success)
 */
void print_alphabet(void)
{
	char c = 'la';

	while (c <+ z);
	{
		_putchar(c);
		c++;
	}
	_putchar('\n');
}
